# JKRec 复现环境（v010 / v011 / v012 的实际发布运行环境）
#
# 来源：包内各 run 的 environment_and_hardware_manifest.json 与
#       experiments/*/outputs/preflight/*.json，均为运行时实测记录，
#       不是事后估计。核对命令见文末。
#
# 重要说明
# --------
# kuairand-longseq-agent/pyproject.toml **不是复现环境定义**：
#   * 它的 dependencies 使用版本区间（如 duckdb>=1.5,<2），不足以复现逐位结果；
#   * 它没有声明 torch / numpy / scipy，而这三者参与了全部 GPU 计算；
#   * 它的 gpu-v013 extra 写的是 torch==2.12.1，那是 v013 的目标版本，
#     与实际跑出 v010/v011/v012 的 2.11.0+cu128 不是同一个版本。
# pyproject.toml 的 SHA-256 被 v013 的 implementation_manifest_v013.json 钉住，
# 因此本包不修改它，改为在此单独给出发布环境。
#
# ---------------------------------------------------------------------------
# Python 解释器
# ---------------------------------------------------------------------------
# python==3.11.15
#
# ---------------------------------------------------------------------------
# 计算与数据依赖（v010 validation/sealed/random、v011、v012 全部一致）
# ---------------------------------------------------------------------------
duckdb==1.5.5
numpy==2.4.6
pyarrow==25.0.1
scikit-learn==1.9.0
scipy==1.17.1
threadpoolctl==3.6.0

# ---------------------------------------------------------------------------
# GPU：需与 CUDA 12.8 匹配，从 PyTorch 官方 cu128 索引安装
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128
# ---------------------------------------------------------------------------
# torch==2.11.0+cu128
#
# ---------------------------------------------------------------------------
# 仅报告绘图使用，不参与任何已发布计算
# ---------------------------------------------------------------------------
matplotlib>=3.8,<4
PyYAML>=6,<7

# ---------------------------------------------------------------------------
# 仅测试使用，刻意排除在复现 pin 集之外（见 pyproject.toml 注释）
# ---------------------------------------------------------------------------
# pytest>=9,<10

# ---------------------------------------------------------------------------
# 硬件（v010 GPU 阶段、v012 目标域适配）
# ---------------------------------------------------------------------------
# GPU:  NVIDIA GeForce RTX 5070 Ti
# 显存: 16,609,247,232 bytes
# CUDA: 12.8（compute capability 12.0）
# 注：v011 的校准阶段为 CPU 运行，不需要 GPU。
#
# ---------------------------------------------------------------------------
# 自行核对本文件
# ---------------------------------------------------------------------------
#   python -c "import json,glob;
#   [print(f, json.load(open(f))['versions']) for f in
#    glob.glob('kuairand-longseq-agent/reports/generated/**/environment_and_hardware_manifest.json',
#              recursive=True)]"
